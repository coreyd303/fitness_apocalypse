<!DOCTYPE html>
<html>
  <head>
    <%= render "shared/meta_tags" %>
  </head>
  <body>
    <div class="container">
      <%= render "shared/header" %>
    </div>

    <main class="container">
      <%= render "shared/flashes" %>
      <div class="container nav--block">
        <%= render "shared/admin/navblock" %>
      </div>
      <div class="container">
        <%= yield %>
      </div>
    </main>

  </body>
</html>